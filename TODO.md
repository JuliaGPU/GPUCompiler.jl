TODO:

The _methods_by_ftype call also (optionally) returns age bounds, which you are required to propagate (usually pretty easy, since it is just min/max calls

_methods_by_ftype also takes mt argument; for overlays of kernels?
also `which` in driver


# put the function and argument types in typevars
# so that we can access it from generated functions

remove that. we just use .f and .tt in the world_age generator
and rename f to ft field
maybe pass sig everywhere?
